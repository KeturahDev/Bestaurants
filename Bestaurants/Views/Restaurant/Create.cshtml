@{
  Layout = "_Layout";
}

@model Bestaurant.Models.Restaurant

<h4>Add a new Restaurant</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)

  @Html.LabelFor(model => model.Description)
  @Html.TextBoxFor(model => model.Description)
  
  @Html.LabelFor(model => model.Address)
  @Html.TextBoxFor(model => model.Address)

  <input id="CuisineId" name="CuisineId" type="hidden" value="@ViewBag.Cuisine.CuisineId"/>
  
  @* @Html.LabelFor(model => model.Delivery)
  @Html.TextBoxFor(model => model.Delivery) *@
  @* <label for="CuisineId">Cuisine:</label>
  @Html.DropDownList("CuisineId"); //drop down list is from viewbag *@

  <input type="submit" value="Bingo"/>
}
<p>@Html.ActionLink("Back to Cuisines", "Index", "Cuisine")</p>